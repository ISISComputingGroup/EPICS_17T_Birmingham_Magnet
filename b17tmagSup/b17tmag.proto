InTerminator = "\n";
OutTerminator = "\n";

ReplyTimeout = 2000;

getField {
    ExtraInput = Ignore;
    out "output";
    # in "%(\$1:FIELD)fT,%f(\$1:CURR)A";
    # in "output:OK:%(\$1FIELD.VAL)fT,%f(\$1CURR.VAL)A";
    # in "output:OK:%(\$1FIELD)fT,%(\$1CURR)fA";
    in "%(\$1FIELD)fT,%(\$1CURR)fA";
}

setField {
    out "abort"; # ADD BACK IN 
    wait 1000;
    out "setfield:%d";
    in "setfield:%d";
}

getHeater {
    out "heater";
    in "heater:OK:%d";
}

# # NEED TO FIX
getPersist {
    out "persistent";
    # in "OK:%(\$1FIELD:PERSIST)fT,%f(\$1CURR:PERSIST)A";
    # in "persistent:OK:%(\$1FIELD:PERSIST)fT,%(\$1CURR:PERSIST)fA";
    in "%(\$1FIELD:PERSIST)fT,%(\$1CURR:PERSIST)fA";
}

getPersistMode {
    out "persistmode";
    # in "persistmode:OK:%s";
    in "%s";
}

setPersistMode {
    ExtraInput = Ignore;
    out "setpersistmode%{ON|OFF}";
    in "%{ON|OFF}";
}

getReady {
    out "ready";
    in "ready:OK:%s";
}

getSensA {
    out "sensA";
    in "sensA:OK:%f";
}

getSensB {
    out "sensB";
    in "sensB:OK:%f";
}

getSet1 {
    out "setpoint1";
    in "setpoint1:OK:%f";
}

getSet2 {
    out "setpoint2";
    in "setpoint2:OK:%f";
}

setSet1 {
    out "set1%f";
    # in "set1:%s"; # OLD
     in "set1:%f"; # NEW
}

setSet2 {
    out "set2%f";
    # in "set2:%s"; # OLD
    in "set2:%f"; # NEW
}


getNeedlePosition {
    out "nv";
    in "nv:OK:%f";
}

getPressure {
    out "pressure";
    # in "pressure:OK:%(\$1NV:PRESSURE:SP:RBV)f,%(\$1NV:PRESSURE)f"; # OLD
    in "pressure:OK:%f,%f";
    # in "pressure:OK:%(\$1NV:PRESSURE:SP:RBV)f";
    # in "%(\$1NV:PRESSURE)f";
}

setPressure {
    out "setpressure %f";
    # in "setpressure:%s"; # origional
    in "setpressure:%f"; # NEW
}
setNeedlePosition {
    out "setposition %f";
    in "setposition:%f";
    # in "setposition:%s"; # OLD
}

getAttoAngle {
    out "attoangle";
    in "attoangle:OK:%f"
}

getHelLevel {
    out "helevel";
    in "%f";
}

setAttoAngle {
    out "setangle%f";
    in "setangle:%f"; #used to be %s
}

getMacroStatus {
    out "getstatus";
    seperator=";";
    # in "getstatus:OK:%s"; # OLD
    in "%s";
}

